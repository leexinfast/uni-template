export const systemPadTop = state => {
    return state.system.padTop
}

export const systemNavBarHeight = state => {
    return state.system.navBarHeight
}

export const systemStatusBarHeight = state => {
    return state.system.statusBarHeight
}

export const systemBoundingHeight = state => {
    return state.system.boundingHeight
}

export const systemBoundingRight = state => {
    return state.system.boundingRight
}

export const getUserInfo = state => {
    return state.user.userInfo
}
